name: Trigger Test

on:
  push:
    branches:
      - schedule-source

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Confirm trigger fired
        run: echo "Trigger fired successfully"
